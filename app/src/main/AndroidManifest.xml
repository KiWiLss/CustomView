<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.magicsoft.customview">
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>
    <uses-permission android:name="android.permission.RESTART_PACKAGES"/>
    

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".MainActivity"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.VolumeActivity"/>
        <activity android:name=".activity.ViewGroupActivity"/>
        <activity android:name=".activity.DispathActivity"/>
        <activity android:name=".activity.listview.ListViewActivity"/>


        <activity android:name=".activity.LayerActivity"/>
        <activity android:name=".activity.Color"/>
        <activity android:name=".activity.PaintActivity"/>
        <activity android:name=".activity.MainActivityA" android:launchMode="singleTop"/>
        <activity android:name=".activity.MainActivityB"/>

        <activity android:name=".activity.pullzoom.PullZoomScroll"/>
    </application>

</manifest>